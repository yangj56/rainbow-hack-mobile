import 'package:flutter/material.dart';

class GroupAchievement extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Group Achievements"),
    );
  }
}
